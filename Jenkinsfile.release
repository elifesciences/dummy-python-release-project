elifePipeline {

    stage 'Checkout', {
        checkout scm master
    }
    
    stage 'Release', {
        elifePypiRelease
    }
    
    stage 'Sandbox release', {
        elifePypiRelease 'test'
    }
        
}

